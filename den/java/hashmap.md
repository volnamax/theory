[[java]]

### `HashMap` в Kotlin (и Java)

`HashMap` — это структура данных, реализующая интерфейс `MutableMap` и использующая хеш-таблицу для хранения пар "ключ-значение". Она обеспечивает быстрый доступ к данным за **O(1)** в среднем случае (при хорошей хеш-функции).

---

### 🔹 Основные характеристики `HashMap`

1. **Хранит пары "ключ-значение" (`key-value`).**
2. **Не гарантирует порядок элементов.** В отличие от `LinkedHashMap`, который сохраняет порядок вставки.
3. **Позволяет `null` в качестве ключа и значения.**
4. **Не допускает дубликатов ключей.** Если добавить элемент с уже существующим ключом — старое значение перезапишется.
5. **Быстрая вставка, удаление и поиск** (при условии отсутствия сильных коллизий).

---

### 🔹 Внутреннее устройство `HashMap`

1. **Основа — массив `Node<K, V>`**, где `Node` содержит `key`, `value`, `hash` и ссылку `next` (для разрешения коллизий).
2. **Хеш-функция вычисляет индекс** по формуле `hash(key) % capacity`.
3. **При коллизии** элементы хранятся в виде **связного списка** (а при высокой нагрузке — в виде **красно-черного дерева** для ускорения поиска).
4. **Реасширение (resize)** происходит при превышении load factor (обычно `0.75`).

---

### 🔹 Когда использовать `HashMap`?

✔ Когда важна **быстрая вставка, удаление и поиск**.  
✔ Когда **неважен порядок элементов**.  
✔ Когда **ключи хорошо хешируются** (желательно `equals()` и `hashCode()` переопределены корректно).

❌ Не подходит, если важен **порядок элементов** → Используйте `LinkedHashMap`.  
❌ Не потокобезопасен → Используйте `ConcurrentHashMap` для многопоточного доступа.

---

### 🔹 Альтернативы

|Коллекция|Особенность|
|---|---|
|`LinkedHashMap`|Сохраняет порядок вставки|
|`TreeMap`|Хранит ключи в отсортированном порядке|
|`ConcurrentHashMap`|Потокобезопасная версия `HashMap`|

# Основные термины

структура данных типа ключ-значение, в виде массива table с элементами Bucket, позволяющая добавлять и получать элементы за О(1)

> equals() - используется для сравнения объектов. для сравнения используется ключ объекта. метод класса Object. 

>hashCode() - возвращает ссылку на объект в виде int. используется для получения номера bucket'а. номер bucket'а - адрес элемента в мапе. hashCode() для null = 0. метод класса Object.

>Bucket - элемент массива HashMap. используется для хранения nodes. две или более ноды могут быть в одном бакете, в таком случае используется LinkedList для их организации. у бакетов разная емкость. *емкость = число бакетов * фактор загрузки* 

**ЕСЛИ ПЕРЕОПРЕДЕЛЯЕТСЯ equals() - ЗНАЧИТ НУЖНО ПЕРЕОПРЕДЕЛИТЬ hashCode()**

# put()/get()

как работает put():
1. ключ объекта проверяется на null. если null, то в значение записывается в table[0].
2. вычисляется хеш код ключа с помощью hashCode(). это значение используется для вычисления индекса в массиве для объекта Entry. чтобы значение хэша было в пределах индексов массива хэш код объекта передают в hash().
3. вызывается IndexFor(hash, table.length) для вычисления позиции индекса для помещения туда объекта Entry.

для get() такая же логика. В тот момент, когда hashmap определит точное совпадение с ключом, переданным в качестве аргумента, он просто вернет значение объекта, хранящееся в текущем объекте Entry.

# Коллизии

два разных объекта могут иметь одно значение хэшкода. в таком случае они будут хранится в одном месте массива, называемое bucket'ом. для разрешения таких ситуаций хэшмапа использует связный список. объект Entry имеет поле next, которое указывает на следующий объект в списке.
в случае возникновении коллизии объекты Entry хранятся в связном списке. когда нужно поместить Entry под определенный индекс, хэшмапа проверяет, есть ли уже там что-то. если ничего нет, то объект помещается в это место. если на этом месте уже есть объект, проверяется значение его поля next. если оно null, то текущий объект становится следующей node в списке. если не null, то проходим по списку, пока не найдем null.

# Улучшения в Java 8

улучшена производительность объектов HashMap с большим количеством коллизий в ключах за счет использования сбалансированных деревьев, а не связанных списков для хранения записей мапы. Основная идея заключается в том, что как только количество элементов в бакете превысит определенный порог, этот бакет перейдет от использования связанного списка к сбалансированному дереву. В случае большого количества коллизий в хэше это позволит повысить производительность в худшем случае с O(n) до O(log n).

В основном, когда бакет становится слишком большим (в настоящее время: TREEIFY_THRESHOLD = 8), HashMap динамически заменяется на TreeMap. Таким образом, вместо O(n) мы получаем O(log n).

Бины (элементы или узлы) TreeNodes можно обходить и использовать так же, как и любые другие, но дополнительно поддерживается ускоренный поиск при переполнении. Однако, поскольку подавляющее большинство бинов при обычном использовании не переполнены, проверка существования бинов дерева может быть отложена в процессе работы табличных методов.

Древовидные бины (т. е. бины, все элементы которых являются TreeNodes) упорядочиваются в основном по hashCode, но в случае связей, если два элемента относятся к одному *class C implements Comparable*<C>, то для упорядочивания используется их метод compareTo().

Поскольку TreeNodes примерно в два раза больше обычных узлов, мы используем их только тогда, когда бины содержат достаточно узлов. А когда они становятся слишком маленькими (из-за удаления или изменения размера), они преобразуются обратно в обычные бины (в настоящее время: UNTREEIFY_THRESHOLD = 6). В системах с хорошо распределенными хэш-кодами пользователей древовидные бины используются редко.